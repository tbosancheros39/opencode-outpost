# v0.16 Release Plan — Corrected Layer 2

## Status: APPROVED 2026-06-06

## Layer 1 (v0.15.2) Status
- Tasks 1.1-1.6: COMPLETE, all FactCheck VERIFIED
- Task 1.7 (npm publish): BLOCKED — npm auth token expired (E401). Needs `npm login`.
- Commit: 969b7ec, tag: v0.15.2
- Changes: ioredis ^5.10.1→^5.11.1, vitest ^4.1.4→^4.1.8, @vitest/coverage-v8 ^4.1.4→^4.1.8

## Layer 2 Corrections (from pre-flight analysis)

### CANCELLED — Already Implemented
- **Task 2.7** (`/commands` keyboard): commands.ts already has full InlineKeyboard with pagination (prev/next), select→confirm→execute→cancel flow. 766 lines.
- **Task 2.8** (inline discoverability): inline-query.ts L431-442 already shows all 6 command suggestions (summarise, eli5, deep-research, steel-man, feynman, devil's-advocate) on empty query via buildCommandSuggestions().

### SKIPPED
- **Task 2.1** (Serena exploration): Already done during pre-flight. All symbol paths confirmed.

### CORRECTED Tasks

**Task 2.2 — Health Probes** (`src/monitoring/health-probes.ts`)
- Only NEW probe: `checkRedisHealth()` — ephemeral ioredis client, ping, disconnect
- OpenCode adapter: thin wrapper around EXISTING `opencodeClient.global.health()`
- Redis usage pattern: `src/queue/queue.ts:5-6` creates Redis from ioredis; `src/app/start-bot-app.ts:58` creates test client
- No shared Redis singleton exists — probe creates ephemeral connection
- Handle: REDIS_ENABLED=false → `{ ok: true, skipped: true }`

**Task 2.3 — Wizard Health Step**
- WRONG: `src/cli.ts` (doesn't exist)
- RIGHT: `src/runtime/bootstrap.ts` (290+ lines), `src/setup/quick-setup.ts`, `src/setup/smart-defaults.ts`
- Subagent must read these files to find the "write .env and done" flow, inject health probe step after .env write

**Task 2.4 — Error Messages** (targeted changes)
- DON'T touch existing OpenCode health display in status.ts (works correctly)
- ADD: Redis connection failure notification during startup (start-bot-app.ts or queue init)
- ENHANCE: status.ts error catch block (L103-105) with actionable fix text
- ADD: i18n keys for new error messages across all 7 locales (en, de, es, fr, ru, zh, bs)

**Task 2.5 — /help Grouped Output** (valid as-is)
- Current: `help.ts` formatHelpText() → flat list via getLocalizedBotCommands()
- Target: categorized sections with bold headers
- Must read `src/bot/commands/definitions.ts` to categorize commands
- Add i18n keys for category headers

**Task 2.6 — /status Enhancement** (additive only)
- Already shows: OpenCode health, version, PID, uptime, agent, model, project, session
- ADD only: Redis health line + queue stats (pending/active from BullMQ or MemoryQueue)
- Import checkRedisHealth from Task 2.2

**Task 2.9 — Build/Test/Bump** (valid as-is)

### Execution Order
```
2.2 → 2.3 → 2.4 → 2.5 → 2.6 → 2.9
```

## Key File Paths (verified by Serena)
| Symbol | File | Lines |
|--------|------|-------|
| config | src/config.ts | 110-177 |
| startCommand | src/bot/commands/start.ts | 15-93 |
| helpCommand | src/bot/commands/help.ts | 26-34 |
| statusCommand | src/bot/commands/status.ts | 14-106 |
| commandsCommand | src/bot/commands/commands.ts | 529-569 |
| inline query handler | src/bot/handlers/inline-query.ts | 24+ |
| health command | src/bot/commands/health.ts | 1+ |
| watchdog | src/monitoring/opencode-watchdog.ts | 1+ |
| system monitor | src/monitoring/system-monitor.ts | 1+ |
| opencode client | src/opencode/client.ts | 1+ |
| queue module | src/queue/queue.ts | 1+ |
| wizard/bootstrap | src/runtime/bootstrap.ts | 290+ |
| quick setup | src/setup/quick-setup.ts | 1+ |
| smart defaults | src/setup/smart-defaults.ts | 1+ |
| i18n locales | src/i18n/{en,de,es,fr,ru,zh,bs}.ts | — |

## i18n Convention
All new user-facing strings need entries in 7 locale files: en, de, es, fr, ru, zh, bs.

## GitHub PAT
`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`
